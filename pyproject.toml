[project]
name = "slideshow-creator"
version = "0.1.0"
description = "A simple program that generates a slideshow of a given length given a set of images and an audio track."
requires-python = ">=3.14"
dependencies = ["json5>=0.15.0"]
